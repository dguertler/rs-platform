name: S&P 600 SmallCap Daten aktualisieren

permissions:
  contents: write

on:
  schedule:
    # Stündlicher Retry 03:30–11:30 MESZ (01:30–09:30 UTC) – siehe update_rs.yml
    # für den Hintergrund. Freshness-Check unten überspringt alle Läufe nach
    # dem ersten erfolgreichen des Tages.
    - cron: '30 1-9 * * 2-6'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0

      - name: Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Datenfrische prüfen
        id: freshness
        run: python3 check_data_freshness.py --files data/rs_smallcap.json >> "$GITHUB_OUTPUT"

      - name: Abhängigkeiten installieren
        if: steps.freshness.outputs.fresh != 'true'
        run: pip install yfinance pandas lxml

      - name: Teil 1 laden (erste Hälfte A–M)
        if: steps.freshness.outputs.fresh != 'true'
        env:
          FMP_API_KEY: ${{ secrets.FMP_API_KEY }}
        run: python smallcap_colab_1.py

      - name: Teil 2 laden (zweite Hälfte N–Z)
        if: steps.freshness.outputs.fresh != 'true'
        env:
          FMP_API_KEY: ${{ secrets.FMP_API_KEY }}
        run: python smallcap_colab_2.py

      - name: Daten zusammenführen
        if: steps.freshness.outputs.fresh != 'true'
        run: python smallcap_merge.py

      - name: JSON nach data/ verschieben und committen
        if: steps.freshness.outputs.fresh != 'true'
        run: |
          mv rs_smallcap.json data/rs_smallcap.json
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add data/rs_smallcap.json
          git diff --staged --quiet || git commit -m "S&P 600 SmallCap Daten aktualisiert"
          git pull --rebase origin master
          git push origin master

      - name: Fehler-Mail senden
        if: failure()
        env:
          SMTP_HOST:      ${{ secrets.SMTP_HOST }}
          SMTP_PORT:      ${{ secrets.SMTP_PORT }}
          SMTP_USER:      ${{ secrets.SMTP_USER }}
          SMTP_PASS:      ${{ secrets.SMTP_PASS }}
          ALERT_EMAIL_TO: ${{ secrets.ALERT_EMAIL_TO }}
          JOB_NAME:       "S&P 600 SmallCap Update (update_smallcap.yml)"
          RUN_URL:        "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        run: python notify_failure.py
